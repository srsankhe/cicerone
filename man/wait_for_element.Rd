% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anchor.R
\name{wait_for_element}
\alias{wait_for_element}
\title{Wait for an element to appear (and become visible) in the DOM}
\usage{
wait_for_element(
  selector,
  timeout = 5000,
  visible = TRUE,
  id = NULL,
  session = NULL
)
}
\arguments{
\item{selector}{A CSS selector, e.g.: \code{"#my_div"} or \code{".my-class"}.}

\item{timeout}{Milliseconds to wait before giving up.}

\item{visible}{Whether the element must also have a non-zero size, not
just exist in the DOM. \code{FALSE} only checks existence -- useful for an
element inside a hidden tab, which exists but has no size until the
tab is shown.}

\item{id}{Identifier of the \verb{\{id\}_cicerone_anchor} input the result is
sent to. Defaults to a sanitised form of \code{selector}: any leading \verb{#}/
\code{.} is stripped, then every remaining non-alphanumeric character is
replaced with \verb{_}. So a call with no explicit \code{id} can be observed
with, e.g., for \code{selector = "#late"}:
\code{observeEvent(input$late_cicerone_anchor, ...)}.}

\item{session}{A valid Shiny session if \code{NULL} the function attempts to
get the session with \code{\link[shiny:getDefaultReactiveDomain]{shiny::getDefaultReactiveDomain()}}.}
}
\value{
Invisibly, the \code{id} the result will arrive under (i.e. the
input to observe is \code{paste0(id, "_cicerone_anchor")}).
}
\description{
Resolves as soon as \code{selector} matches an element on the page, without
needing a \link{Cicerone} tour or \link{Hints}. Use it to gate a \verb{$start()} call,
or any other server-side logic, on a piece of UI that renders
asynchronously: an \code{insertUI()}, a slow render, an element inside a
hidden tab.
}
\details{
The result arrives on the \verb{\{id\}_cicerone_anchor} Shiny input (event
priority), a list with \code{selector}, \code{found}, \code{visible} and \code{elapsed}
(milliseconds). \code{found} is \code{TRUE} once \code{selector} matches an element;
\code{visible} is \code{TRUE} once that element's \code{getBoundingClientRect()} has
both width and height greater than 0. If \code{timeout} elapses first,
\code{found}/\code{visible} report whatever was true at that point (both \code{FALSE}
if the selector never matched at all).
}
\seealso{
\link{cicerone_inputs}
}
