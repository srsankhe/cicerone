% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/persist.R
\name{tour_state}
\alias{tour_state}
\title{Read persisted tour state from the request cookie}
\usage{
tour_state(session = NULL, id = NULL)
}
\arguments{
\item{session}{A valid Shiny session if \code{NULL} the function
attempts to get the session with \code{\link[shiny:getDefaultReactiveDomain]{shiny::getDefaultReactiveDomain()}}.}

\item{id}{A tour's \code{id}. If \code{NULL}, returns every persisted tour's
record, as a named list keyed by \code{id}.}
}
\value{
If \code{id} is given: that tour's record (\code{list(v, status, idx, n, t)}), or \code{NULL} if there is none. If \code{id} is \code{NULL}: a named list
of every persisted tour's record (possibly empty).
}
\description{
Reads the \code{cicerone} cookie set by a \link{Cicerone} tour created with
\code{persist = "cookie"} (see \code{?Cicerone}), parsed from
\code{session$request$HTTP_COOKIE} -- the raw \verb{Cookie:} header Shiny
captured at WebSocket handshake time.
}
\details{
This is a synchronous, server-side read, available before any UI is
sent (e.g. from inside the server function, to decide what to render
on first paint). It reflects whatever cookie the browser sent when
the current session connected or reconnected; it does NOT reflect a
write a tour made earlier in the SAME live session (the browser does
not resend its \code{Cookie} header mid-connection). For that, use the
live \verb{\{id\}_cicerone_seen} input instead (see \link{cicerone_inputs}).

\code{tour_state()} does not check a record's \code{v} (version) against
anything -- it has no tour object to compare against, so it returns
whatever is stored, unfiltered. A \link{Cicerone} tour's own version check
(which decides \verb{\{id\}_cicerone_seen}, \code{run_once} suppression, and
\code{resume}) happens separately, at \verb{$init()}/\verb{$start()}.
}
\seealso{
\link{Cicerone}, \link{cicerone_inputs}
}
