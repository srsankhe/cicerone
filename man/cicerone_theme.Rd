% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{cicerone_theme}
\alias{cicerone_theme}
\title{Theme cicerone popovers}
\usage{
cicerone_theme(
  surface = NULL,
  text = NULL,
  muted = NULL,
  accent = NULL,
  radius = NULL,
  font_size = NULL,
  shadow = NULL,
  btn_bg = NULL,
  btn_text = NULL,
  progress_fg = NULL,
  progress_bg = NULL,
  selector = ".driver-popover",
  preset = NULL
)
}
\arguments{
\item{surface, text, muted, accent, radius, font_size, shadow, btn_bg, btn_text, progress_fg, progress_bg}{CSS values for the corresponding custom property, e.g.:
\code{accent = "#ff0000"}, \code{radius = "10px"}. \code{NULL} (the default) leaves
that property unset.}

\item{selector}{CSS selector to set the properties on, e.g. a
\code{popover_class} passed to \link{Cicerone}/\code{\link[=initialise]{initialise()}}/\code{\link[=highlight]{highlight()}}, to
theme one tour only. Defaults to \code{.driver-popover}: every popover in
the app.}

\item{preset}{A built-in set of defaults to start from. Currently only
\code{"bootstrap"} is supported: it maps every property to the matching
bslib/Bootstrap 5.3+ CSS variable (\verb{--bs-*}), so the theme follows the
app's bslib theme, including dark mode, automatically. \code{surface},
\code{text}, \code{accent}, \code{radius}, \code{font_size} and \code{shadow} map to the
matching semantic Bootstrap token (\code{--bs-body-bg}, \code{--bs-body-color},
\code{--bs-primary}, \code{--bs-border-radius}, \code{--bs-body-font-size},
\code{--bs-box-shadow}); \code{muted} maps to \code{--bs-secondary-color}; \code{btn_bg}
and \code{progress_fg} map to \code{--bs-primary} (buttons and the filled
progress indicator read as the theme's primary color); \code{btn_text}
maps to \code{--bs-white}; \code{progress_bg} maps to \code{--bs-secondary-bg} (the
same token Bootstrap's own \code{.progress} track uses). Explicit arguments
override the preset's value for that one property.}
}
\value{
An \link[htmltools:tags]{htmltools::tags} \verb{<style>} element.
}
\description{
Set the CSS custom properties \code{custom.css} reads for popover styling,
without hand-editing CSS. Returns a \verb{<style>} tag to place anywhere in
the UI; the rule it emits targets a selector, not one element
instance, so a single call covers every popover that selector matches.
}
\section{CSS custom properties}{

Each argument maps to one property, set on \code{selector} (\code{.driver-popover}
by default). Only supplied arguments are emitted; every other property
keeps driver.js's own default look.
\describe{
\item{\code{surface}}{\code{--cicerone-surface}: popover background, and the
one visible border side of the popover arrow (kept equal to the
surface so the arrow reads as a solid-colour pointer).}
\item{\code{text}}{\code{--cicerone-text}: title/description text color.}
\item{\code{muted}}{\code{--cicerone-muted}: close button icon color.}
\item{\code{accent}}{\code{--cicerone-accent}: Next button background.}
\item{\code{radius}}{\code{--cicerone-radius}: popover corner radius.}
\item{\code{font_size}}{\code{--cicerone-font-size}: description font size.}
\item{\code{shadow}}{\code{--cicerone-shadow}: popover box shadow.}
\item{\code{btn_bg}}{\code{--cicerone-btn-bg}: footer button background
(Previous/Close; Next starts from \code{accent} instead).}
\item{\code{btn_text}}{\code{--cicerone-btn-text}: footer button text color.}
\item{\code{progress_fg}}{\code{--cicerone-progress-fg}: progress text color,
and the filled portion of a \code{\link[=step]{step()}}/\code{\link[=initialise]{initialise()}}
\code{progress_style = "bar"}/\code{"dots"}.}
\item{\code{progress_bg}}{\code{--cicerone-progress-bg}: the unfilled track of
a \code{progress_style = "bar"}/\code{"dots"}.}
}
}

\examples{
library(shiny)

ui <- fluidPage(
  use_cicerone(),
  cicerone_theme(accent = "#ff0000", radius = "10px")
)

}
