% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anchor.R
\name{destroy_all}
\alias{destroy_all}
\title{Destroy every active tour}
\usage{
destroy_all(session = NULL)
}
\arguments{
\item{session}{A valid Shiny session if \code{NULL} the function attempts to
get the session with \code{\link[shiny:getDefaultReactiveDomain]{shiny::getDefaultReactiveDomain()}}.}
}
\value{
Invisibly, the session.
}
\description{
Destroys every currently active \link{Cicerone} tour on the page, regardless
of \code{id}. Unlike a single tour's \verb{$reset()}/\verb{$destroy()}, this is not
scoped to one tour: use it as a single, session-wide teardown, e.g.
before navigating to a new page section or starting an unrelated flow.
Every affected tour's \verb{_ended} input fires with \code{reason = "programmatic"} (see \link{cicerone_inputs}). Hints are not affected -- they
are not tours.
}
\seealso{
\link{Cicerone}, \link{cicerone_inputs}
}
